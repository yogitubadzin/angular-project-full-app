import { NotLoggedInDirective } from './not-logged-in.directive';

describe('NotLoggedInDirective', () => {
  it('should create an instance', () => {
    // const directive = new NotLoggedInDirective();
    // expect(directive).toBeTruthy();
  });
});
